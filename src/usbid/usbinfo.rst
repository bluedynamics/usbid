
prepare
=======

imports::
    >>> from usbid.usbinfo import USBInfo
 
    >>> #interact(locals())    
    >>> info = USBInfo()
    >>> info._parse_usbids()
    >>> pprint(info._device_classes)

    